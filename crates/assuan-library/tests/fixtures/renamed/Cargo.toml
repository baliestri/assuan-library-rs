[package]
name = "assuan-renamed-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
library = { package = "assuan-library", path = "../../..", default-features = false, features = ["server", "macros", "sexpr"] }

[workspace]
